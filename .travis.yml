language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t hawley/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true hawley/docker-react npm run test -- --coverage
